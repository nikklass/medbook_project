import { MostVisitsHighlightDirective } from './most-visits-highlight.directive';

describe('MostVisitsHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MostVisitsHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
